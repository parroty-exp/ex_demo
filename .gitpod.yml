tasks:
  - init: |
      sudo apt install elixir -y && mix local.hex --force
    command: |
      echo 'TODO: start app'
      git config --local user.name parroty-exp
      git config --local user.email parroty-exp@example.com
